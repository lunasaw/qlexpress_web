package com.ql.qlexpress.web.config;

import org.springframework.context.annotation.Configuration;

/**
 * LiteFlow 配置类
 * LiteFlow 使用 Spring Boot Starter 自动配置，此类用于扩展配置
 */
@Configuration
public class LiteFlowConfig {
    // LiteFlow 通过 liteflow-spring-boot-starter 自动配置
    // 如需自定义配置，可在此添加 Bean
}
