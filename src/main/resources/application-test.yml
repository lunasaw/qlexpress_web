# QLExpress4 可视化编排 Web 应用 - 测试环境配置
qlexpress:
  # 脚本执行配置
  script:
    # 执行超时时间（毫秒）
    timeout: 10000
    # 是否启用缓存
    cache-enabled: true
    # 是否启用表达式追踪
    trace-enabled: false
    # 是否开启调试模式
    debug: false
